apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: router-alert-rules
  namespace: monitoring
  labels:
    release: prometheus
spec:
  {{- $.Files.Get "rules/router/alerts.yaml" | nindent 2}}
