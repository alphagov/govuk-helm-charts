fastly-exporter:
  envValueFrom:
    FASTLY_API_TOKEN:
      secretKeyRef:
        key: "token"
        name: "govuk/fastly/prometheus-exporter"
  options:
    service-allowlist: '.*[Ii]ntegration.*'
