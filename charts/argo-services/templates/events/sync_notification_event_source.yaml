apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: sync-notification
spec:
  # TODO: service should be replaced before production
  # See [trello card](https://trello.com/c/ExJgQV9N/715-productionalize-the-webhook-argo-event-source-configuration)
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    argocd:
      port: "12000"
      endpoint: /argocd
      method: POST
