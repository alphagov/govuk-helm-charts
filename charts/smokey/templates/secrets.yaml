---
apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: smokey
spec:
  refreshInterval: 2m
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: {{ .Release.Name }}
  dataFrom:
  - key: govuk/smokey
