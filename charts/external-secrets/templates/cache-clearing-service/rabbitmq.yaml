apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cache-clearing-service-rabbitmq
  labels:
    {{- include "external-secrets.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/description: >
      RabbitMQ connection string for cache-clearing-service. Fields in
      SecretsManager are username, password, host, port.
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval }}
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    deletionPolicy: {{ .Values.externalSecrets.deletionPolicy }}
    name: cache-clearing-service-rabbitmq
    template:
      data:
        RABBITMQ_URL: '{{ $.Files.Get "externalsecrets-templates/amqp-conn-string.tpl" | trim }}'
  dataFrom:
    - extract:
        key: govuk/cache-clearing-service/rabbitmq
