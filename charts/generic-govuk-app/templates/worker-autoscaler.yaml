{{- $fullName := include "generic-govuk-app.fullname" . }}
{{- with .Values.workerPodAutoscaling -}}
{{- if .enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .name | default $fullName }}-worker
spec:
  {{- toYaml .spec | nindent 2 }}
{{- end -}}
{{- end -}}
