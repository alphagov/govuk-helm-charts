{{- $fullName := include "generic-govuk-app.fullname" . }}
{{- with .Values.podAutoscaling -}}
{{- if .name.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .name | default $fullName }}
spec:
  {{- toYaml .spec | nindent 2 }}
{{- end -}}
{{- end -}}
