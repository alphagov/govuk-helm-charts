{{- if .Values.licensify.frontend.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "licensify.frontend.fullname" . }}
  labels:
    {{- include "licensify.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "licensify.frontend.fullname" . }}
  minReplicas: {{ .Values.licensify.frontend.autoscaling.minCount }}
  maxReplicas: {{ .Values.licensify.frontend.autoscaling.maxCount }}
  metrics:
    {{- if .Values.licensify.frontend.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.licensify.frontend.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.licensify.frontend.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.licensify.frontend.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
