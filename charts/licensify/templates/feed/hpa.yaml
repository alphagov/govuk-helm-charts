{{- if .Values.licensify.feed.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "licensify.feed.fullname" . }}
  labels:
    {{- include "licensify.feed.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "licensify.feed.fullname" . }}
  minReplicas: {{ .Values.licensify.feed.autoscaling.minCount }}
  maxReplicas: {{ .Values.licensify.feed.autoscaling.maxCount }}
  metrics:
    {{- if .Values.licensify.feed.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.licensify.feed.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.licensify.feed.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.licensify.feed.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
