apiVersion: v1
kind: Service
metadata:
  name: {{ include "authenticating-proxy.fullname" . }}
  labels:
    {{- include "authenticating-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authenticating-proxy.selectorLabels" . | nindent 4 }}
