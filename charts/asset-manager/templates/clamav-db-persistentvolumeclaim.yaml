apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clamav-db-efs
  labels:
    {{- include "asset-manager.labels" . | nindent 4 }}
    app.kubernetes.io/component: clamav-pvc
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: clamav-db-efs
  resources:
    requests:
      storage: 1Gi
